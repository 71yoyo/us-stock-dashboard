# 이 파일은 값의 형식만 보여 주는 예시입니다. 실제 API 키는 .env에만 입력합니다.
# .env는 .gitignore로 보호되며 GitHub에 올라가지 않습니다.

# 향후 Node.js 서버를 시작할 때 사용할 포트
PORT=3000

# 사용할 금융 데이터 API의 키 (예: Finnhub, Polygon, Twelve Data 등)
MARKET_DATA_API_KEY=your_api_key_here

# SQLite 데이터베이스의 로컬 경로
DATABASE_PATH=./data/us-stock-pro.sqlite
