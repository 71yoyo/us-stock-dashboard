# US Stock Pro 프로젝트 규칙

## 1. 기술 스택과 전환 원칙

- 목표 기술 스택은 **React(함수형 컴포넌트·Hooks)**, **Node.js**, **TypeScript**다. 새 화면과 기능은 가능하면 TypeScript로 구현해 타입 오류를 줄인다.
- 스타일은 컴포넌트 단위 관리와 반응형 UI에 적합한 **Tailwind CSS**를 우선 추천한다. 단, 디자인 토큰이나 복잡한 동적 스타일이 많아져 CSS-in-JS가 더 적합한 경우에만 styled-components 등을 제안한다.
- 현재 프로젝트는 빌드 과정 없는 정적 HTML/CSS/JavaScript 앱이다. 사용자의 명시적 요청 없이 React·TypeScript로 전체를 한 번에 전환하지 않는다.
- 구조 개편 전까지는 `index.html`에 화면 구조, `style.css`에 스타일, `app.js`에 동작과 상태 관리를 둔다. 전환 작업 시에는 기존 기능과 저장 데이터를 유지할 수 있는 단계적 마이그레이션 계획을 먼저 제시한다.

## 2. 구조와 데이터

- 사용자 데이터는 브라우저 `localStorage`에 저장한다. 현재 키는 `stock_app_pin`, `stock_app_watchlist`, `stock_app_holdings`다.
- `localStorage`와 JSON 백업 파일의 기존 형식을 깨뜨리지 않는다. 형식 변경이 필요하면 이전 데이터를 읽는 마이그레이션 또는 안전한 기본값을 제공한다.
- 실제 시세·환율·차트 데이터를 도입할 때는 데이터 출처, 갱신 주기, API 비용·제한, 연결 실패 시 대체 화면을 명확히 구현한다. 예시·랜덤 데이터는 실제 데이터처럼 표시하지 않는다.
- PIN은 클라이언트 측 편의 잠금일 뿐 강한 보안 수단이 아니다. 로그인이나 민감 데이터 보호가 필요하면 서버 인증과 안전한 저장 방식을 별도로 제안한다.

## 3. 코드 품질

- **읽기 쉬운 코드가 최우선이다.** 변수·함수·컴포넌트 이름은 역할과 의미가 드러나게 짓고, 조건문 중첩과 한 함수에 여러 책임을 넣는 것을 피한다.
- **주석은 필수다.** 코드가 무엇을 하는지와 왜 이 방식을 선택했는지를 한국어로 설명한다. 자명한 코드의 반복 설명은 피하고, 판단 근거·제약·부작용처럼 유지보수에 필요한 의도를 남긴다.
- **기능별 모듈화가 원칙이다.** 하나의 파일이 과도하게 길어지면 화면, 공통 UI, 상태 관리, API·저장소 로직 단위로 분리한다. 단순한 기능을 불필요하게 여러 파일로 쪼개지는 않는다.
- **에러 처리를 항상 고려한다.** 외부 API, 파일 읽기, 브라우저 저장소, 사용자 입력 등 실패할 수 있는 모든 경로에 유효성 검사와 오류 처리를 둔다. 오류 메시지는 사용자가 다음 행동을 알 수 있게 한국어로 작성한다.
- HTML의 요소 ID·클래스를 변경하거나 삭제할 때는 연결된 JavaScript, 스타일, 접근성 속성까지 함께 수정한다.
- DOM 이벤트는 초기화 시 한 번만 등록한다. 다시 렌더링하거나 화면 전환해도 이벤트가 중복 등록되지 않게 한다.

## 4. 메뉴와 UI 규칙

- 메인메뉴는 `1`, `2`, `3`처럼 숫자를 표시하고, 서브메뉴는 부모 번호를 포함한 `1-1`, `1-2`, `2-1` 형식으로 표기한다.
- 메뉴 번호는 화면 이동·문서화·수정 범위를 빠르게 찾기 위한 식별자다. 메뉴명이나 순서가 바뀌면 관련 화면, 이벤트, 안내 문구를 함께 갱신한다.
- 데스크톱과 모바일에서 주요 흐름이 모두 작동하도록 반응형 UI를 유지한다.

## 5. 커뮤니케이션

- 사용자에게 제공하는 설명과 코드 주석은 한국어로 작성한다.
- 답변은 원칙적으로 **결론/해결책 → 코드 또는 변경 사항 → 상세 설명** 순서로 구성한다.
- 초보자도 이해할 수 있는 쉬운 표현을 쓰되, 데이터 흐름·비즈니스 규칙·구조적 이유는 생략하지 않는다.
- 요청에 보안, 데이터 손실, 비용, 성능, 유지보수성 같은 리스크가 있거나 더 적합한 대안이 있으면 구현 전에 간결하게 알리고 선택지를 제안한다.

## 6. 완료 전 확인

- 수정한 화면·버튼·입력 폼의 정상 동작과 브라우저 콘솔 오류를 확인한다.
- PIN 잠금·해제, 관심종목 추가·삭제·순서 변경, 포트폴리오 저장, JSON 백업·복원을 확인한다.
- `localStorage`에 저장한 데이터가 새로고침 뒤에도 유지되는지 확인한다.
- 외부 CDN 또는 API 연결이 실패해도 앱의 핵심 화면과 기존 데이터가 손상되지 않는지 확인한다.

## 7. Williams %R 전략 기록

- Williams %R 신호·색·표시 문구를 바꾸기 전에 `docs/williams-r-strategy.md`를 읽는다.
- 사용자가 추가로 제공한 책 내용과 합의한 변경 사항은 출처·이유·검증 방법을 구분해 이 문서에 기록하고, 관련 코드와 테스트도 함께 갱신한다.
